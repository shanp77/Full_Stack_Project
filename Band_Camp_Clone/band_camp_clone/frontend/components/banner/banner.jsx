import React from 'react';
import { connect } from 'react-redux';

const msp = state => {
  return {

  };
};

const mdp = dispatch => {
  return {

  };
};

const Banner = ( (props) => {

  return (
    <div id="banner">
      
    </div>
  )
});
connect(msp, mdp)(Banner);